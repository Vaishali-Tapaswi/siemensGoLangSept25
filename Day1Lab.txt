Lab4 - Lab4.go (accept input from user and if, for 
package main

import "fmt"

func main() {
	
	name := "aaa"
	fmt.Println(name)
	fmt.Println("Enter your name ")
	cnt, err := fmt.Scanln(&name)
	fmt.Println(cnt, err)
	fmt.Println(name)
	
	//marks := 20
	var marks int
	fmt.Println(marks)
	fmt.Println("Enter your marks ")
	cnt, err = fmt.Scanln(&marks)
	fmt.Println(cnt, err)
	fmt.Println(marks)
	
	no := 100/marks 
	fmt.Println("NO is ", no)
	fmt.Println("Finished")
}
	try giving different marks - 90, 90vvv, vvv 
Write two files
	Asgn1
		Accept a number from user and print Fibonacci series till that number
	if condition { 
	} else {
}
Init, Condition and Post
	for i := 0; i < 10; i++
For is Go’s while
	for sum < 1000 {
Infinite Loop
	for {

	

	

Lab3 - Lab3.go (Functions and returns, multiple returns 
      create Lab3.go 

      package main
      import "fmt"
      func calc(x,y int ) (int, int) {
	      fmt.Println("in add with ", x , " and ", y)
	      //return x+y, x-y
      }
      func main(){
      	n1, n2  := calc(1000,400)
      	fmt.Println("Sum = ", n1, " Sub = ", n2)
      	n1, n22  := calc(10000,4000)
      	fmt.Println("Sum = ", n1, " Sub = ", n22)
      }
      cmd>go run Lab3.go 
            observe error (wants vs have), uncomment 
            observe n1 and n22 on line n1, n22  := calc(10000,4000)
                  change n1 and n22 to old/new variables 
                        if both the variables are existing -> error 
                        if any one is new -> no error 
     move calc function in new file Lab3Helper.go 
     cmd>go run lab3.go 
            error for calc not found 
     cmd>go run lab3.go Lab3Helper.go 
          error  (observe error - package , import)
      cmd>go build lab3.go Lab3Helper.go 
            1 exe


Lab2 - Lab2.go 
      package main
      
      import (
      	"fmt"
      	"os"
      	)
      func main() {	
      	// no intialization
      	var a1 string
      	// with intialization, data type is inferred
      	var a2 = "aaa"
      	// short syntax
      	var a3, n1 = "aaa",300.39
      	/*
      	a3 := "aaaa"
      	n1:=300.39
      	*/
      	fmt.Println("Hello World !!")
      	fmt.Println(os.Args)
      	fmt.Println(a1)
      	fmt.Println(a2)
      	fmt.Println(a3)
      	fmt.Printf("%T ", n1)
      	n1 = 330.330
      	fmt.Println(n1)
      }
      Observe - 1. Variable/import declared but not used 
                2. n1 = 3000/0 ->
                3. n2 := 1000/n1 (where n1 is zero)
                4. modify printf  to use %T twice with single variable

Lab1 - install go 
      go version (any thing > than 1.11 
      create a folder demo1, 
      create a file demo1\Lab1.go 
        package main
        
        import "fmt"
        
        func main() {
        	fmt.Println("Hello World !!")
        }
  cmd>go build Lab1.go 
      check exe created, run
  cmd> go run Lab1.go 
  cmd>go fmt Lab1.go 
